{
  "name": "smm-action-logger",
  "version": "7.3.0",
  "private": true,
  "description": "Браузерный рекордер действий: пройти сценарий руками и получить всё, из чего потом пишется скрипт/автоматизация.",
  "scripts": {
    "lint": "node --check action-logger-v7.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8'))\"",
    "test": "node --test test/*.test.mjs",
    "test:integration": "node --test test/integration/*.test.mjs",
    "check": "npm run lint && npm test && npm run test:integration"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
